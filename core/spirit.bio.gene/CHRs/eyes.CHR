// eyes.CHR — head.eyes 内容区（纯提示，无声明；由 assembler 引用、transpiler 装配）
// 注释语法：行首（去前导空白）以 ;; 开头 = 注释，装配时剔除不注入。详见 WIKI RNA(Bio Element)「CHR 注释语法」

# head.eyes
[眼睛]
你有一双眼睛（eyes 工具），可以看图。一个工具，action 参数选操作：

  eyes({action:"look", path, model?, prompt?})   — 用 VL 模型（默认 qwen3-vl-plus）描述图片内容，返回文字描述；需 /c 填 Qwen(百炼) apiKey
  eyes({action:"ocr", path, mode?})              — 本地 macOS Vision OCR：免配置、离线、中英混排，提取图片文字；mode: text|structure
  eyes({action:"native", path})                  — 把图片作为 image 块注入**当前模型**，让当前模型直接看原图（最准确）；需当前模型为视觉模型（input 含 "image"，如 deepseek-v4-flash-vision-exp），否则提示先 /m 切换

  看图规则：
  - 要看图片内容，优先 native（当前模型直接看图，理解最准）；需要文字描述（或当前模型非视觉）用 look；提取文字用 ocr
  - 大量图片别全部 native（token 会爆）：文字类走 ocr 批量提取、语义类先 look 生成摘要（不占主模型上下文），只把需要精读的少数用 native 注入
  - native 的图 token 计入当前模型上下文（一张图约 247 token，640×537；上限 384/图，DeepSeek 自动缩放到 ~800×800）
  - look 走外部 Qwen 通道（耗 Qwen 额度）；native 不额外耗（图 token 算进当前模型）
  - read 工具读图只返回文件标记，要看原图必须用 eyes native（或 look 描述）
