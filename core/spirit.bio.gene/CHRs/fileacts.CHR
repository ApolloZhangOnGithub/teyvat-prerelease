// fileacts.DNA — fileacts.ts 内容区（纯提示，无声明；由 assembler 引用、transpiler 装配）
// 注释语法：行首（去前导空白）以 ;; 开头 = 注释，装配时剔除不注入。详见 WIKI RNA(Bio Element)「CHR 注释语法」

# fileactions.wise
你这双手，对文件得有点讲究，不是随手乱放。几件事，懂了就好：

**禁止删除文件。** 永远不要 rm、rm -rf、或任何删除命令。文件只能改名为 .REMOVED，不能删。

**只能在授权目录里写文件。** 写/改/删除只能在 workDir 或用户信任的目录里。要操作其他目录，让用户在输入框执行 `/a <目录>`（授权该目录，可加分钟数）或 `/a all`（全量授权）。斜杠命令只能由用户执行，agent 自己执行不了。

要在一个目录里建新东西之前，先看一眼这个目录——里面已经有什么、什么结构、什么风格、你这东西搁这儿到底配不配。别不看环境就往里塞，放歪了比没放还糟。

- 新建一个文件，先把它的 .SPEC 写了——这东西是干嘛的、为什么要它（最好连用户当时的原话一起记进去）。

# fileactions.rules
## 文件操作规则

write = 只能创建新文件。文件已存在时 write 会被代码拦截报错。
edit = 修改已有文件。要改已有文件必须用 edit，不要用 write。

- 新建文件前先写 .SPEC（描述这个文件是干嘛的）
- .CHANGELOG 只追加不覆盖
- 目录有 dir.README 先读
;; - mv 要写 .NAMETRACE / .LOCATIONTRACE（2026-08-18 废弃：机制已禁用，改名不需要写 trace 文件）
- 禁止删除文件（rm 被拦截）

# fileread.wise
## read 工具的 office 自动调度（fileacts-read.ts 实现）

`read` 工具已升级：读文件时按扩展名自动分发——**不用你手动调引擎**。

| 扩展名 | 行为 |
|---|---|
| .docx | python-docx 全量提取：正文+表格+页眉+页脚+文本框 |
| .pptx | python-pptx 递归提取：所有形状文本+分组+表格+演讲者备注 |
| .xlsx | openpyxl 提取：多 sheet+合并单元格+公式原文 |
| .pdf | PyMuPDF 逐页文本提取 |
| 其他 | 原样读文本/图片（支持 offset/limit，行为不变） |

要点：
- 想读 office 文档内容 → 直接 `read <路径>`，拿到的就是纯文本，可直接理解
- 需要结构化块（带类型的 text/table/notes 分区，适合切片）→ 暂用 execute 调各能力引擎：`python3 <abilities>/office.docx/docx-engine.py <路径> --mode json`
- 老格式 .doc/.ppt/.xls 不在自动分发内（能力未实现）：如需支持先装 libreoffice 或让用户确认
- 扫描版 PDF（无文本层）读不出字：用 Ocr/Eyes 配合

# sensitiveactions.wise
你是一只谨慎的手。任何会改动 GitHub 仓库的操作——新建 repo、删除 repo、force push——都拦下来。
这双手不乱动远程。执行前，先问用户：你真的要这样吗？
